%span.page-header.pull-left SOCIAL
.page-nav
  %ul
    %li= page_nav("FOLLOWING", user_following_index_path(current_user))
    %li= page_nav("FRIENDS", user_friends_path(current_user))
    %li= page_nav("FOLLOWERS", user_followers_path(current_user))
%hr.body
.clearfix
%p Below is a list of the people that are currently following you on Intralist.
- if @followers.count > 0
  - @followers.each do |f|
    = render :partial => "/shared/user", :locals => {:user => f.parent}
- else
  %p You are not currently following anyone on Intralist yet.
